//TODO: (5) @Medium Convert the dev-form validation function to a re-useable function

Change Over
-------------------
1. User names are in consistent and the db will need to be modified.
Note: Work around in the getAllUsers controller\users to create a fullname if one is missing
users will still not be able to log in uptil the paswords are adjusted.

Remove passwords
db.users.update({}, {$unset: {salt:1, hashed_pwd:1, roles:1, firstName:1, lasteName:1}},{multi:true})

Resources : 
1. React SkyLight is a simple react component for modals and dialogs. Powerful, lightweight and customizable design.
